# Icons

Place your **swipe-up** icon here:

- **`swipe-up.svg`** – used on the home page “Swipe up” button (shown twice, left and right of the text).

Use any size; the component displays it at 20×20 (size-5). PNG also works: use `swipe-up.png` and update `HomePage.tsx` to reference `/icons/swipe-up.png` if you prefer.
